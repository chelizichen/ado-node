import { RpcServerController,Register } from "ado-node";

/**
 * @description <%= description %>
 */
@RpcServerController("<%= interFace  %>")
class <%= interFace %> {
    <% for(let v in method){%>

    @Register("<%= v %>")
    async <%= v %>(<%- JSON.stringify(method[v]['req']).replaceAll("\"","").replaceAll("\{","").replaceAll("\}","")  %>):Promise<<%- JSON.stringify(method[v]['res']).replaceAll("\"","") %>>{
        throw new Error("")
    }

    <% } %>
}



export{
    <%= interFace %>
}