import { RpcServerController,Register } from "ado-node";

/**
 * @description <%= description %>
 */
@RpcServerController("<%= interFace  %>")
class <%= interFace %> {
    <% for(let v in method){%>

    @Register("<%= v %>")
    async <%= v %>(<%- req(v)  %>):Promise<<%- res(v) %>>{
        throw new Error("")
    }

    <% } %>
}



export{
    <%= interFace %>
}