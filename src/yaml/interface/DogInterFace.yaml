interFace:
  # 接口名称
  name: DogInterFace
  # rpc 服务 远程地址（客户端需要）
  remote: http://127.0.0.1:9000
  # 注释
  description: "用于编写 Dog 接口的配置文件"

  client:
    controller: dog
method:
  woff:
    req: WoffRequest
    res: WoffResponse
  eat: 
    req: EatRequest
    res: EatResponse

    